spring:
    flyway:
        enabled: true
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:filmesdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false;MODE=MYSQL
        username: sa
        password: